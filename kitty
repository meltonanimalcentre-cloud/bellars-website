export const metadata = {
  title: "Bellars Grooming",
  description: "Premier pet styling",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body style={{ margin: 0 }}>{children}</body>
    </html>
  );
}
